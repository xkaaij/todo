Xanders app
<hr>
<%= "Notice:#{flash[:notice]}" %>
<hr>
No todos yet
<hr>
<%= link_to "Create a new todo", new_todo_path %>

<ul>
<% @todos.each do |todo| %>
  <li><%=	todo.name %><%= button_to "delete", todo, :method => :delete %></li>
<% end %>
</ul>

